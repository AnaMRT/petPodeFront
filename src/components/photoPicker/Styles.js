import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  title: {
    fontSize: 18,
    fontWeight: "bold",
    marginBottom: 10,
    color: "#6B4226",
  },

  subtitle: {
    fontWeight: "600",
    marginBottom: 5,
  },

  avatar: {
    width: 60,
    height: 60,
    borderRadius: 30,
    marginRight: 10,
    borderWidth: 2,
    borderColor: "#A3B18A",
  },

  options: {
    marginTop: 20,
  },
});

export default styles;